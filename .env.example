# Owner-only direct Postgres connection used by migrations and cloud refresh.
# Never expose this value through VITE_* or commit a populated .env file.
SUPABASE_DB_URL=postgresql://postgres.project-ref:password@host:5432/postgres
