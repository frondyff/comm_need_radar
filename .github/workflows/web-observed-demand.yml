name: Web Observed Demand

on:
  workflow_dispatch:
    inputs:
      as_of_date:
        description: Optional UTC reporting date in YYYY-MM-DD format
        required: false
        type: string
      window_days:
        description: Rolling analytics window
        required: true
        default: "90"
        type: string
      publish:
        description: Atomically replace observed/V2 tables after reviewing a dry run
        required: true
        default: false
        type: boolean
  schedule:
    - cron: "20 6 * * 1"

permissions:
  contents: read

concurrency:
  group: comm-need-radar-web-observed-demand
  cancel-in-progress: false

jobs:
  build:
    if: github.event_name == 'workflow_dispatch' || vars.WEB_OBSERVED_DEMAND_ENABLED == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
      SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
      PUBLISH_REQUESTED: ${{ (github.event_name == 'workflow_dispatch' && inputs.publish == true) || (github.event_name == 'schedule' && vars.WEB_OBSERVED_DEMAND_PUBLISH_ENABLED == 'true') }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -r requirements.txt
      - name: Build observed demand and V2
        shell: bash
        run: |
          as_of_date="${{ inputs.as_of_date }}"
          window_days="${{ inputs.window_days || '90' }}"
          if [[ -z "$as_of_date" ]]; then
            as_of_date="$(date -u +%F)"
          fi
          args=(
            --from-supabase
            --as-of-date "$as_of_date"
            --window-days "$window_days"
          )
          if [[ "$PUBLISH_REQUESTED" == "true" ]]; then
            args+=(--publish)
          fi
          python scripts/build_web_observed_demand.py "${args[@]}"
          echo "publish_requested=$PUBLISH_REQUESTED" >> "$GITHUB_STEP_SUMMARY"
      - uses: actions/upload-artifact@v4
        with:
          name: web-observed-demand-quality
          path: data/derived/web_observed_demand/
          if-no-files-found: error
          retention-days: 30
